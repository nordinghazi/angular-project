import { FiltroCochesPipe } from './filtro-coches.pipe';

describe('FiltroCochesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCochesPipe();
    expect(pipe).toBeTruthy();
  });
});
